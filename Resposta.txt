SELECT DISTINCT c.CustomerID, c.*
FROM Customers c LEFT JOIN Orders o JOIN OrderDetails od JOIN Products p JOIN Suppliers sp
on c.CustomerID = o.CustomerID AND o.OrderID = od.OrderID AND od.ProductID = p.ProductID AND p.SupplierID = sp.SupplierID AND sp.Country = 'Brazil'